# Components

The component directory contains all reusable components.
Rules for creating components:

- Generic components that assume NO domain are stored in `./ui`
- Reusable domain components are stored directly under the parent directory, categorized by feature.
